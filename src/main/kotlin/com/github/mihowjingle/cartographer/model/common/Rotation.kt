package com.github.mihowjingle.cartographer.model.common

/**
 * Rotation in degrees (can be negative), z axis = vertical axis
 */
data class Rotation(val xAxis: Number, val zAxis: Number, val yAxis: Number)